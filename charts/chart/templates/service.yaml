apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart.fullname" . }}-podinfo
  labels:
  {{- include "chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.podinfo.type }}
  selector:
    app: podinfo
  {{- include "chart.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.podinfo.ports | toYaml | nindent 2 -}}