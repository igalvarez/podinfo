apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart1.fullname" . }}-podinfo
  labels:
  {{- include "chart1.labels" . | nindent 4 }}
spec:
  type: {{ .Values.podinfo.type }}
  selector:
    app: podinfo
  {{- include "chart1.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.podinfo.ports | toYaml | nindent 2 -}}