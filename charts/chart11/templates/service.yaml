apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart11.fullname" . }}-podinfo
  labels:
  {{- include "chart11.labels" . | nindent 4 }}
spec:
  type: {{ .Values.podinfo.type }}
  selector:
    app: podinfo
  {{- include "chart11.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.podinfo.ports | toYaml | nindent 2 -}}