apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart2.fullname" . }}-git-chart2-podinfo
  labels:
  {{- include "chart2.labels" . | nindent 4 }}
spec:
  type: {{ .Values.gitChart2Podinfo.type }}
  selector:
    app: podinfo
  {{- include "chart2.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.gitChart2Podinfo.ports | toYaml | nindent 2 -}}