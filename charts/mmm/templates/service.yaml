apiVersion: v1
kind: Service
metadata:
  name: {{ include "mmm.fullname" . }}-podinfo
  labels:
  {{- include "mmm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.podinfo.type }}
  selector:
    app: podinfo
  {{- include "mmm.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.podinfo.ports | toYaml | nindent 2 -}}