apiVersion: v1
kind: Service
metadata:
  name: {{ include "mychart.fullname" . }}-git-chart2-podinfo
  labels:
  {{- include "mychart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.gitChart2Podinfo.type }}
  selector:
    app: podinfo
  {{- include "mychart.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.gitChart2Podinfo.ports | toYaml | nindent 2 -}}