apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "9d51.fullname" . }}-
  labels:
  {{- include "9d51.labels" . | nindent 4 }}
spec:
  host: podinfo-demo.apps.fqywlnxx.eastus.aroapp.io
  port:
    targetPort: http
  to:
    kind: Service
    name: podinfo