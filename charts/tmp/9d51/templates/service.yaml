apiVersion: v1
kind: Service
metadata:
  name: {{ include "9d51.fullname" . }}-podinfo
  labels:
  {{- include "9d51.labels" . | nindent 4 }}
spec:
  type: {{ .Values.podinfo.type }}
  selector:
    app: podinfo
  {{- include "9d51.selectorLabels" . | nindent 4 }}
  ports:
	{{- .Values.podinfo.ports | toYaml | nindent 2 -}}