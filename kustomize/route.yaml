apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: podinfo
spec:
  host: podinfo-chart-podinfo-chart2.apps.fqywlnxx.eastus.aroapp.io
  port:
    targetPort: http
  to:
    kind: Service
    name: podinfo
