apiVersion: v1
items:
- apiVersion: route.openshift.io/v1
  kind: Route
  spec:
    host: podinfo-demo.apps.fqywlnxx.eastus.aroapp.io
    port:
      targetPort: http
    to:
      kind: Service
      name: podinfo
